# Creator Workbench Modules

This directory contains the creator-facing lexicon pipeline and workbench code
for PipeWorks.

These modules are build-time and authoring-time tools. They prepare corpus
material, expose the maintained web workbench, and generate package artifacts
for downstream runtime import.

They are not runtime-serving modules and are not the canonical API boundary.
